# Core Flask
Flask==3.0.0

# Authentication & Security
Werkzeug==3.0.1
authlib==1.3.0
requests==2.31.0

# Email Support
Flask-Mail==0.9.1

# Environment Variables
python-dotenv==1.0.0

# Database (SQLite is built-in with Python)

# SMS/OTP (already in use)
# requests is already listed above

# Optional but recommended
gunicorn==21.2.0  # For production deployment
